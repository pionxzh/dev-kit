import { createTheme } from '@mui/material/styles/index.js'

export const theme = createTheme({
  palette: {
    primary: {
      main: '#06A09B'
    },
    secondary: {
      main: '#2196f3'
    }
  }
})
